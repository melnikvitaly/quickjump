<div>
	<div>Edit page for {{provider.name}}</div>
	<form name="form" novalidate class="css-form">
		<div>
			<span>Enabled</span><input  type="checkbox" ng-model="provider.enabled" />
		</div>
		<div>
			<span>Name : </span><input  type="text" ng-model="provider.name"  required/>
			<span class="error" ng-show="provider.name.$error.required">Required!</span>
		</div>
		<ng-include src="editUrl"></ng-include >
	</form>	
	<div>
        <button ng-click="save()">Save</button>
        <button ng-click="back()">Back</button>
    </div>	
     <style type="text/css">
      .css-form input.ng-invalid.ng-dirty {
        background-color: #FA787E;
      }
    
      .css-form input.ng-valid.ng-dirty {
        background-color: #78FA89;
      }
    </style>
</div>
